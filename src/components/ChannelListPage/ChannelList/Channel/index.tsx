import { FC } from 'react';

const Channel: FC = () => {

  return (
    <div>
      <span>Channel title</span>
      <span>subs count</span>
      <button>Edit</button>
      <button>Test</button>
      <button>Run</button>
      <button>Stop</button>
    </div>
  );
}

export default Channel;